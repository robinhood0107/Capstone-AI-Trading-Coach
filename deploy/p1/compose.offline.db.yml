name: capstone-p1-offline-demo

services:
  runtime-netns: {pull_policy: never}
  api-edge: {pull_policy: never}
  postgres: {pull_policy: never}
  redis: {pull_policy: never}
  migrate: {pull_policy: never}
  identity-bootstrap: {pull_policy: never}
  python-worker: {pull_policy: never}
  synthetic-async-smoke: {pull_policy: never}
  spring-api: {pull_policy: never}
