# 배포 검증 스크립트

| 스크립트 | 결과 | 근거 |
|---|---|---|
| `tests/verification/deployment_verification_runner.py` | PASS 22 / BLOCKED 6 / NOT_APPLICABLE 6 | `artifacts/decision-platform/verification/report.json` |
| `deploy/p1/verify-public-rag-seed` | **PASS** — `PUBLIC_RAG_SEED_VERIFY=PASS` | |
| `contracts/verify_p1_compose_supply_handoff.py` | **PASS** — `OWNER_POST_TEAM_CODE_REQUIRED=0` | |
| `contracts/verify_s5_signal_runtime_transition.py` | **PASS** — 종료코드 0 | |
| `contracts/verify_p1_full_app_assets.py` | **정상 거절** — `RETURN:EVIDENCE_MODE` 외 | 합성 번들을 실제 산출물로 받아들이지 않는다 |
| `deploy/p1/mock_certification_guard.py` | **FAIL** — `KIS_MOCK_CERTIFICATION_DIRTY_WORKTREE` | 작업 tree가 깨끗해야 통과한다 |
| return-engine pytest 2파일 | **PASS** — 3 passed | |
| `scripts/lint-static-analysis.sh` | **NOT_RUN** — `shellcheck` 미설치 | 핀 고정 도구 8종이 이 머신에 없다 |
| 대시보드 Playwright `live-compose.spec.ts` | **PASS** — 2 passed | RAG v2 화면 검증 추가 |

## 배포 검증 러너에서 고친 하드코딩

예전에는 RAG v2 검색과 Vertex 생성 두 항목의 판정이 코드에 `BLOCKED`로 박혀 있었다. 배포 설정이
바뀌어도 보고서는 계속 같은 말을 했다. 지금은 **실행 중인 컨테이너에게 물어** 그 답을 판정으로
쓴다. Team B validator 항목도 물질화된 번들이 있으면 `PASS`가 되도록 바꿨다.

암호화 답변 이력 항목도 무조건 `PASS`였다. 지금은 조회가 실제로 숫자를 돌려줬을 때만 PASS다.

## 지금 돌릴 수 없는 것

| 스크립트 | 이유 |
|---|---|
| `deploy/p1/verify-release` | 서명된 릴리스 번들 + `cosign`/`gh` 필요. 번들은 릴리스 워크플로만 만든다 |
| `deploy/p1/verify-offline-runtime` | 같음 |
| `deploy/p1/verify-team-b-oci` | 실제 Team B GHCR digest와 Sigstore 영수증이 없다 |
