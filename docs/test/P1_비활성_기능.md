# 꺼져 있어야 하는 것

`tests/e2e/disabled_features_e2e.py` · 증거 `artifacts/decision-platform/e2e/disabled-features.json`

"안 켰다"는 설정 문장이고 "닫혀 있다"는 관측이다. 둘은 다르다.

| 기능 | 방식 | 결과 |
|---|---|---|
| 네 기능의 런타임 플래그 | stack | PASS — 넷 다 `false` |
| 꺼진 기능은 표면에 없다 | stack | PASS — operation 경로에 해당 없음 |
| LightGBM 은퇴 유지 | stack | PASS — V74가 회수한 권한이 0건 |
| 브로커리지는 모의다 | stack | PASS — `KIS_MOCK` / `INTERNAL_PAPER` |
| 뉴스 거부권 전송 fail-closed | in-process | PASS — 물리 호출 0 |

## 넷을 켜지 않기로 한 근거

| 기능 | 확인된 이력 | 처리 |
|---|---|---|
| SearXNG (`RAG_WEB_ENABLED`) | V71에서 실제 실행. DuckDuckGo CAPTCHA/`ALL_ENGINES` 장애 관측 | `KNOWN_DEGRADED_NONBLOCKING` 유지, 앞으로 사용 안 함 |
| Strong LLM / MCP (S4.9) | 2026-08-15 live 검증 완료. 그러나 P1 owner-first 범위 밖 | `RESEARCH_ONLY` 확정 |
| 금융공학 gRPC (S6) | S6.1~S6.5 구현 완료지만 offline only, educational REST | 교육용 REST 유지, 배포에서 off |

**결정적 근거**: 1.0.0을 여는 16개 hard gate 어디에도 이 넷이 없다. 릴리스 범위 밖으로 설계된
것이지 배선을 안 한 것이 아니다.

## 은퇴·금지 6항목

`LightGBM 신호`, `Naver 뉴스 provider`, `cross-market`, `GDELT 온라인`, `KIS 실계좌 주문`,
`강제청산·물타기·미체결 재가격`. 배포 검증 러너가 `NOT_APPLICABLE`로 유지하며 그 근거를 함께
남긴다. LightGBM은 V74의 REVOKE가 지금도 유효함을 위 표에서 실측한다.

## 이름은 닮았지만 꺼진 기능이 아닌 표면 하나

`/api/v2/strong-llm/settings`는 `/strong-llm` 접두사를 갖지만 이 검사의 대상이 아니다. S4.9
agent 자체는 여전히 꺼져 있고 그 endpoint도 없다. 이것은 **어떤 모델을 쓸지 고르는** 설정
쓰기이고 언제나 열려 있어야 한다 — provider를 바꾸는 길이 배포 환경변수뿐이면 그 선택이
사용자의 것이 아니라 운영자의 것이 된다.

예외는 접두사가 아니라 **경로 하나로** 두었다. 접두사로 열어 두면 나중에 진짜 agent endpoint가
같은 접두사로 들어와도 이 검사를 통과한다.

## 아직 이 증거가 의미하지 않는 것

- 실제 Team B 산출물이 아니다. 여기서 쓴 번들은 합성 golden이고 `evidence_mode`가 그 사실을 말한다.
- 실제 Team A UI가 아니다. 대시보드는 현재 tree의 화면이며 최종본이 아니다.
- 실계좌 거래가 아니다. KIS Live 호출은 영구 금지이고 이 기록 어디에도 그 경로가 없다.
- 시장이 열린 시간의 실체결이 아니다. 그 항목은 `P1_장시간_의존_항목.md`가 따로 다룬다.
