use_default_settings:
  engines:
    keep_only:
      - duckduckgo
      - brave
      - mojeek
      - qwant
      - wikipedia

general:
  debug: false
  instance_name: Capstone S4.9 Internal Search

search:
  safe_search: 1
  autocomplete: ""
  formats:
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false
  image_proxy: false

ui:
  static_use_hash: true

enabled_plugins: []
