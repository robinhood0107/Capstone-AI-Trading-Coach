*.csv
*.json
*.pyc
__pycache__/

# 현재 Team B 수신본은 preview 재현 입력/출력으로 원본 그대로 보존한다.
!data/stock/005930.KS.csv
!artifacts/005930.KS.json

# yfinance 런타임 수집물과 exact-10 중간 산출물은 커밋하지 않는다.
# .csv/.json 은 위에서 이미 막혔고 parquet/safetensors/pth 만 열려 있었다.
*.parquet
*.safetensors
*.pth
# 위 수신본과 짝을 이루는 preview 모델은 이미 추적 중이므로 그대로 둔다.
!data/model/005930.KS_lstm.pth
